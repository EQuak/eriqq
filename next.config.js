/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // async redirects() {
  //   return [{ source: '/', destination: '/homne', permanent: false }];
  // },
};

module.exports = nextConfig;
